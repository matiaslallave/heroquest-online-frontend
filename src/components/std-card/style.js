import { makeStyles } from "@material-ui/core/styles";


export const useStyles = makeStyles({
    cardContainer: {
      maxWidth: 315,
      maxHeight: 400
    },
    media: {
      height: 280,
    },
  });